apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb-order
  labels:
    app: mongodb-order
spec:
  serviceName: mongodb-order
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-order
  template:
    metadata:
      labels:
        app: mongodb-order
    spec:
      containers:
      - name: mongodb
        image: mongo:6.0
        ports:
        - containerPort: 27017
          name: mongodb
        volumeMounts:
        - name: mongodb-order-data
          mountPath: /data/db
        env:
        - name: MONGO_INITDB_DATABASE
          value: orderdb
  volumeClaimTemplates:
  - metadata:
      name: mongodb-order-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
